product:
  type: lib
  platforms: [jvm, android, iosArm64, iosSimulatorArm64, iosX64]

#settings:
#  kotlin:
#    freeCompilerArgs:
#      - -Xexplicit-api=strict
# applies to AMPER-4212

dependencies:
  - $libs.bignum
  - $libs.kotlinx.atomicfu
  - $libs.kotlinx.collections.immutable

settings:
  kotlin:
    freeCompilerArgs: [ -Xcontext-parameters ]
